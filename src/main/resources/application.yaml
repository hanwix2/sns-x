spring:
  application:
    name: sns-x
  datasource:
    url: jdbc:h2:mem:snsx
    driver-class-name: org.h2.Driver
    username: sa
    password: null
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  session:
    store-type: redis
    timeout: 30m
  data:
    redis:
      host: localhost
      port: 6379
server:
  servlet:
    session:
      cookie:
        http-only: true
        same-site: lax
